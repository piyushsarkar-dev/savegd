import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Save Gd",
  description: "Save GD || Home",
};

const page = () => {
  return (
    <section className="grid h-[90dvh] place-items-center">
      
    </section>
  );
};

export default page;
